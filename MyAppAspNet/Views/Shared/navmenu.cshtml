@model IEnumerable<MyAppAspNet.Models.MenuApp>


@foreach (var item in Model)
{
    if (item.Role.UserRoles.Where(a => a.allow_view == true).Where(a => a.id_user == 16).FirstOrDefault() != null)
    {
        @Html.Partial("~/Views/Shared/itemmenu.cshtml", item);
    }
}
